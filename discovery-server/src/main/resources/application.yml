server:
  port: 8761

spring:
  application:
    name: discovery-server

eureka:
  client:
    # the registry itself neither registers nor fetches
    register-with-eureka: false
    fetch-registry: false
